name: Secrets workflows

on:
  pull_request:
    branches: [ "main" ]

env: 
  API_SCPROJ_PATH: "./src/GitHubActionsDotNet.Api/GitHubActionsDotNet.Api.csproj"

jobs:
  secrets:
    name: Secrets Demo
    runs-on: ubuntu-latest
    env: 
      JOB_NAME: "Secrets"

    steps:
      - name: echo environment variable
        run: echo "the value of $JOB_NAME is $API_SCPROJ_PATH"

  new_job:
    name: Secrets Demo
    runs-on: ubuntu-latest
    env: 
      JOB_NAME: "new_job"

    steps:
      - name: echo environment variable
        run: echo "the value of $JOB_NAME is $API_SCPROJ_PATH"